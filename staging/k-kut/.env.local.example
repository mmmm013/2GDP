# Copy this file to .env.local before running locally.
# In Vercel: Settings → Environment Variables → add each key.

# ── Supabase (K-KUT project) ──────────────────────────────────────────────────
# Project URL — used by both client and server
NEXT_PUBLIC_SUPABASE_URL=https://vwlzubxshjjonabpeagd.supabase.co

# Anon (public) key — safe to expose client-side
NEXT_PUBLIC_SUPABASE_ANON_KEY=
